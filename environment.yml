name: smartgrid
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - torch>=2.0.0
    - pandas>=2.0.0
    - numpy>=1.24.0
    - scikit-learn>=1.3.0
    - xgboost==2.1.1       # Pin exactly — XGBoost 3.x breaks SHAP 0.49.x
    - shap>=0.49.0
    - matplotlib>=3.7.0
    - seaborn>=0.12.0
    - scipy>=1.11.0
    - ads-evt>=1.0.0
    - jupyterlab>=4.0.0
    - ipykernel
    - tqdm
